# Design references

These files are the original local visual references supplied for the BUILI build.
Runtime code does not import them; production-ready copies live under
`apps/web/public` and the implemented design system lives in `apps/web/src`.
